package router

import (
  os "os"

  gin "github.com/gin-gonic/gin"
	gorm "github.com/jinzhu/gorm"
)

var userHeader string

func init() {
	if userHeader = os.ExpandEnv("$HEADER_USER"); userHeader == "" {
		userHeader = "x-user"
	}
}

func InstallRouter(db gorm.DB, router *gin.Engine) {<% t.forEach(function(table) { %>
  Install<%=table%>Router(db, router)<%})%>
}
